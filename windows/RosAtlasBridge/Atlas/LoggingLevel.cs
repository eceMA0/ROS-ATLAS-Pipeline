namespace RosAtlasBridge.Atlas;

/// <summary>Verbosity for <see cref="Logger"/>: a message is printed when its level is at or below the configured one.</summary>
internal enum LoggingLevel
{
    Error,
    Warning,
    Info,
    Debug,
    Trace,
}
